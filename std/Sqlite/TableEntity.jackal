class Entity {
    init(handle, tableName) {
        this.handle = handle
        this.tableName = tableName
        this.selectedColumns = "*"
        this.sql = "SELECT * FROM " + tableName
        this.joinClause = ""
        this.whereClause = ""
    }
}