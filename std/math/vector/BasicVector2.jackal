class BasicVector2 {
    init(x, y) {
        this.x = x
        this.y = y
    }

    func dot(v : BasicVector2) -> BasicVector2 = 
        BasicVector2((this.x * v.x), (this.y * v.y))

    func add(v : BasicVector2) -> BasicVector2 = 
        BasicVector2((this.x + v.x) , (this.y + v.y))

    
}