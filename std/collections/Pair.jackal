class Map of [K, V] {
    
    init(){
        this.data = {}; 
    }

    function set(key: K, val: V) {
        this.data[key.toString()] = val;
    }

    function get(key: K): V {
        return this.data[key.toString()];
    }

}